/* V36 — authoritative command views. Fixes UUID ids, real client ids, and prevents local hydration from hiding Supabase commands. */
(function(){
 const wait=(fn,n=0)=>{if(window.__EDDU_SB&&window.EDDU_AUTH?.getUser?.()&&typeof db!=='undefined'&&typeof app!=='undefined'&&typeof render==='function')fn();else if(n<240)setTimeout(()=>wait(fn,n+1),250)};
 const idArg=id=>JSON.stringify(String(id));
 const currentClientId=()=>{const u=window.EDDU_AUTH?.getUser?.();const c=(db.clients||[]).find(x=>u&&String(x.user_id)===String(u.id))||db.clients?.find(x=>String(x.id)===String(app.selectedClient));return c?.id??db.clients?.[0]?.id??null};
 const serviceFor=it=>{if(typeof svc==='function'){const s=svc(it.serviceId);if(s)return s;const s2=svc(it.dbServiceId);if(s2)return s2}return(window.SERVICES||[]).find(s=>String(s.dbId)===String(it.dbServiceId)||String(s.id)===String(it.serviceId))||null};
 function orderCardV36(o){const x=typeof orderCalc==='function'?orderCalc(o):{sub:Number(o.subtotal||0),discount:Number(o.discount||0),net:Number(o.total||0),cost:Number(o.cost||0),commission:Number(o.commission||0),profit:Number(o.profit||0)};const oid=idArg(o.id);return `<div class="card" style="margin:10px 0;background:#fbfaf8"><div class="page-head" style="margin-bottom:8px"><div><h3>${esc(o.client||'Cliente')} <span class="badge ${o.open?'warn':o.payment==='paid'?'ok':'warn'}">${o.open?'EM ABERTO':o.payment==='paid'?'PAGO':'AGUARDANDO PAGAMENTO'}</span></h3><span class="muted">Profissional: ${esc(o.prof||'')}</span></div><strong class="money">${money(x.net)}</strong></div><div class="grid4"><div><small class="muted">Subtotal</small><br><b>${money(x.sub)}</b></div><div><small class="muted">Desconto</small><br><b>${money(x.discount)}</b></div><div><small class="muted">Custo</small><br><b>${money(x.cost)}</b></div><div><small class="muted">Lucro</small><br><b>${money(x.profit)}</b></div></div><br><div class="actions"><button class="btn" onclick="viewOrder(${oid})">Visualizar comanda</button><button class="btn" onclick="openOrder(${oid})">Editar comanda</button>${o.open?`<button class="btn primary" onclick="closeOrder(${oid})">Fechar comanda e escolher pagamento</button>`:o.payment==='pending'?`<button class="btn warn" onclick="openPaymentModal(${oid})">Receber pagamento</button>`:''}</div></div>`}
 function ordersV36(){const all=Array.isArray(db.orders)?db.orders.filter(o=>o&&o.id):[];const open=all.filter(o=>o.open),pay=all.filter(o=>!o.open&&o.payment!=='paid'),paid=all.filter(o=>!o.open&&o.payment==='paid');return `<div class="content"><div class="page-head"><div><h1>Comandas</h1><p>Controle completo de serviço, desconto, custo, comissão e lucro.</p></div><button class="btn primary" onclick="newOrder()">+ Abrir comanda</button></div><div class="grid3"><div class="card"><div class="eyebrow">Em aberto</div><strong style="font-size:28px">${open.length}</strong><p class="muted">Atendimentos em andamento</p></div><div class="card"><div class="eyebrow">Aguardando pagamento</div><strong style="font-size:28px">${pay.length}</strong><p class="muted">Comandas enviadas ao cliente</p></div><div class="card"><div class="eyebrow">Pagas</div><strong style="font-size:28px">${paid.length}</strong><p class="muted">Histórico concluído</p></div></div><br>${[['COMANDAS ABERTAS',open,'open'],['AGUARDANDO PAGAMENTO',pay,'pay'],['PAGAS',paid,'paid']].map(([t,l,k])=>`<div class="card" style="margin-bottom:16px"><h2>${t}</h2>${l.length?l.map(orderCardV36).join(''):'<p class="muted">Nenhuma comanda nesta etapa.</p>'}</div>`).join('')}</div>`}
 function clientPaymentsV36(){const cid=currentClientId();const list=(db.orders||[]).filter(o=>String(o.clientId)===String(cid));return `${header()}<main><div class="client-shell"><div class="page-head"><div><h1>Pagamentos</h1><p>Confira suas comandas e acompanhe o status dos pagamentos.</p></div></div>${list.length?list.map(o=>{const x=typeof orderCalc==='function'?orderCalc(o):{net:Number(o.total||0),discount:Number(o.discount||0)};const oid=idArg(o.id);const status=o.open?'Aguardando fechamento':o.payment==='paid'?'Pago':'Aguardando pagamento';const action=o.open?'':o.payment!=='paid'?`<button class="btn primary" onclick="clientPayOrder(${oid})">Pagar agora</button>`:`<button class="btn outline" onclick="viewClientOrder(${oid})">Ver comprovante</button>`;return `<div class="card payment-card" style="margin-bottom:12px"><div class="payment-hero"><small>Total da comanda</small><strong>${money(x.net)}</strong></div><br><div class="page-head" style="margin-bottom:8px"><div><h2>${esc(o.client||'Cliente')}</h2><span class="badge ${o.payment==='paid'?'ok':'warn'}">${status}</span></div></div><p class="muted">${(o.items||[]).map(i=>serviceFor(i)?.n||i.description||'Serviço').filter(Boolean).join(' • ')||'Serviços não informados'}</p><div class="summary"><div class="summary-row"><span>Desconto</span><b>${money(x.discount)}</b></div></div><br><div class="command-actions"><button class="btn outline" onclick="viewClientOrder(${oid})">Ver comanda</button>${action}</div></div>`}).join(''):'<div class="card"><p class="muted">Nenhuma comanda encontrada para este cliente.</p></div>'}</div></main>${clientBottom()}`}
 wait(()=>{window.orders=ordersV36;window.clientPayments=clientPaymentsV36;window.EDDU_COMMAND_VIEW_V36=true;if(window.EDDU_COMMAND_LIST_SYNC?.sync)setTimeout(()=>window.EDDU_COMMAND_LIST_SYNC.sync(true),300)});
})();
