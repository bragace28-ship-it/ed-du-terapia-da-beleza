import {useEffect,useState} from 'react'
import type {FormEvent,ReactNode} from 'react'
import {supabase} from './lib/supabaseClient'
import {APP_COMMIT,APP_ENV,APP_VERSION} from './version'
import {calculateBestPaymentGateway} from './lib/paymentEngine'
import {addCommandItem,closeCommandOnline,createCommand,getAppointments,getClientByUser,getClients,getCommands,getFinancial,getProfile,getServices,markCommandPaidPdv,recalculateCommand} from './lib/data'
import type {Profile} from './lib/data'
import './styles/app.css'
const money=new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'});const dt=new Intl.DateTimeFormat('pt-BR',{dateStyle:'short',timeStyle:'short'})
type View='home'|'agenda'|'clients'|'commands'|'services'|'finance'|'reports'|'documents'|'gateways'|'settings'|'support'
function ErrorBox({text}:{text:string}){return <div className="error-banner">{text}</div>}function Empty({title,text}:{title:string;text:string}){return <div className="empty"><b>{title}</b><span>{text}</span></div>}function Loading(){return <div className="loading">Carregando dados reais…</div>}
function Footer(){return <footer className="technical">ED & DU · V{APP_VERSION} · {APP_ENV} · {APP_COMMIT}</footer>}
export default function App(){const[profile,setProfile]=useState<Profile|null>(null);const[uid,setUid]=useState('');const[boot,setBoot]=useState(true);const[err,setErr]=useState('');useEffect(()=>{let alive=true;supabase.auth.getSession().then(async({data})=>{if(data.session?.user){const p=await getProfile(data.session.user.id);if(alive&&p?.active){setUid(data.session.user.id);setProfile(p)}}}).catch(e=>alive&&setErr(e.message)).finally(()=>alive&&setBoot(false));const{data}=supabase.auth.onAuthStateChange(async(_,s)=>{if(!s?.user){setProfile(null);setUid('');return}try{const p=await getProfile(s.user.id);if(p?.active){setUid(s.user.id);setProfile(p)}}catch(e){setErr(e instanceof Error?e.message:'Falha ao carregar perfil')}});return()=>{alive=false;data.subscription.unsubscribe()}},[]);if(boot)return <div className="app-loading"><b>ED & DU</b><span>TERAPIA DA BELEZA</span><Loading/></div>;if(!profile)return <Login error={err} setError={setErr}/>;return <Shell profile={profile} uid={uid}/>}
function Login({error,setError}:{error:string;setError:(s:string)=>void}){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[signup,setSignup]=useState(false),[busy,setBusy]=useState(false),[message,setMessage]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');setMessage('');try{if(signup){const{data,error}=await supabase.auth.signUp({email:email.trim(),password});if(error)throw error;if(!data.session)setMessage('Cadastro criado. Confirme o e-mail para entrar.')}else{const{error}=await supabase.auth.signInWithPassword({email:email.trim(),password});if(error)throw error}}catch(x){setError(x instanceof Error?x.message:'Falha na autenticação.')}finally{setBusy(false)}};return <main className="auth-page"><section className="auth-card"><div className="brand-mark">ED & DU</div><div className="brand-sub">TERAPIA DA BELEZA</div><h1>{signup?'Criar conta':'Entrar no aplicativo'}</h1><p className="muted">Acesso seguro por Supabase Auth.</p>{error&&<ErrorBox text={error}/>} {message&&<div className="success-banner">{message}</div>}<form onSubmit={submit}><label>E-mail<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Senha<input type="password" required minLength={6} value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="primary-btn full" disabled={busy}>{busy?'Processando…':signup?'Criar conta':'Entrar'}</button></form><button className="link-btn" onClick={()=>setSignup(!signup)}>{signup?'Já tenho uma conta':'Criar conta de cliente'}</button><Footer/></section></main>}
function Shell({profile,uid}:{profile:Profile;uid:string}){const pro=profile.role!=='client';const[view,setView]=useState<View>('home');const nav=pro?[['home','Início'],['agenda','Agenda'],['clients','Clientes'],['commands','Comandas'],['finance','Financeiro']]:[['home','Início'],['agenda','Agendar'],['documents','Docs'],['support','Suporte']];const signout=async()=>{await supabase.auth.signOut()};return <div className="app-shell"><header className="topbar"><button className="brand" onClick={()=>setView('home')}><b>ED & DU</b><span>TERAPIA DA BELEZA</span></button>{pro&&<div className="role-pill"><span className="active">Profissional</span><span>Cliente</span></div>}<div className="top-actions">♧<span className="avatar">{(profile.full_name||'E')[0]}</span></div></header><div className="body-layout"><aside className="sidebar"><div className="side-title">{pro?'Área do profissional':'Área do cliente'}</div>{nav.map(([id,label])=><button key={id} className={`side-nav ${view===id?'active':''}`} onClick={()=>setView(id as View)}>{label}</button>)}{pro&&<><div className="side-title extra">Gestão</div>{[['services','Serviços'],['reports','Relatórios'],['gateways','Gateways'],['settings','Configurações']].map(([id,label])=><button key={id} className="side-nav" onClick={()=>setView(id as View)}>{label}</button>)}</>}<button className="side-nav logout" onClick={()=>void signout()}>Sair</button></aside><main className="main"><div className="content"><Route view={view} pro={pro} uid={uid} profile={profile}/></div></main></div><nav className="bottom-nav">{nav.map(([id,label])=><button key={id} className={view===id?'active':''} onClick={()=>setView(id as View)}><b>{label}</b></button>)}</nav><Footer/></div>}
function Head({eyebrow,title,subtitle,action}:{eyebrow:string;title:string;subtitle?:string;action?:ReactNode}){return <div className="page-head"><div><div className="eyebrow">{eyebrow}</div><h1>{title}</h1>{subtitle&&<p>{subtitle}</p>}</div>{action}</div>}
function Route({view,pro,uid,profile}:{view:View;pro:boolean;uid:string;profile:Profile}){if(view==='home')return <Dashboard pro={pro} uid={uid} setView={()=>{}}/>;if(view==='agenda')return <Agenda pro={pro}/>;if(view==='clients'&&pro)return <Clients/>;if(view==='commands'&&pro)return <Commands/>;if(view==='services'&&pro)return <Services/>;if(view==='finance'&&pro)return <Finance/>;if(view==='reports'&&pro)return <Simple title="Relatórios" text="Os indicadores serão derivados exclusivamente dos lançamentos reais."/>;if(view==='documents')return <Simple title={pro?'Documentos do salão':'Meus documentos'} text="Nenhum documento real disponível para este usuário."/>;if(view==='gateways'&&pro)return <Gateways/>;if(view==='settings'&&pro)return <Simple title="Configurações" text={`Perfil autenticado: ${profile.full_name||'sem nome'} · ${profile.role}.`}/>;return <Simple title="Suporte" text="Configure o contato oficial do salão para habilitar o suporte WhatsApp."/>}
function Dashboard({pro,uid,setView}:{pro:boolean;uid:string;setView:()=>void}){const[a,setA]=useState<any[]>([]),[c,setC]=useState<any[]>([]),[client,setClient]=useState<any>(null),[loading,setLoading]=useState(true);useEffect(()=>{Promise.all([getAppointments(),getCommands(),getClientByUser(uid)]).then(([aa,cc,cl])=>{setA(aa);setC(cc);setClient(cl)}).finally(()=>setLoading(false))},[uid]);if(loading)return <Loading/>;const today=new Date().toISOString().slice(0,10),todayA=a.filter(x=>String(x.starts_at).slice(0,10)===today),rev=c.filter(x=>['paid','closed'].includes(x.status)).reduce((s,x)=>s+Number(x.total||0),0);if(!pro)return <><Head eyebrow="ÁREA DO CLIENTE" title={`Olá, ${client?.name||'cliente'}.`} subtitle="Cuide de você. Seu próximo momento de bem-estar está aqui."/><div className="loyalty-card"><span>PONTOS FIDELIDADE</span><strong>{client?.loyalty_points??0}</strong><small>pontos registrados</small></div><div className="hero-grid"><Action title="Agendar atendimento" text="Escolha serviço e horário" onClick={setView}/><Action title="Meus agendamentos" text={`${a.filter(x=>x.client_id===client?.id).length} registro(s) real(is)`} onClick={setView}/><Action title="Meus documentos" text="Comprovantes e documentos" onClick={setView}/><Action title="Suporte" text="Atendimento do salão" onClick={setView}/></div></>;return <><Head eyebrow="ÁREA DO PROFISSIONAL" title={`Olá, ${uid? 'profissional':''}.`} subtitle="Sua agenda, clientes e resultados em um só lugar."/><div className="metric-grid"><Metric v={todayA.length} l="Atendimentos hoje"/><Metric v={c.length} l="Comandas"/><Metric v={money.format(rev)} l="Faturamento pago"/><Metric v="—" l="Avaliação média"/></div><div className="points-panel"><div><span>PONTOS PROFISSIONAL</span><strong>—</strong><small>Sem pontuação cadastrada.</small></div></div><h2>Acesso rápido</h2><div className="quick-grid"><Action title="Nova Comanda" text="Registrar serviço" onClick={setView}/><Action title="Minha Agenda" text="Ver atendimentos" onClick={setView}/><Action title="Meus Clientes" text="Histórico e perfil" onClick={setView}/><Action title="Relatórios" text="Vendas e performance" onClick={setView}/></div></>}
function Metric({v,l}:{v:ReactNode;l:string}){return <div className="metric-card"><strong>{v}</strong><small>{l}</small></div>}function Action({title,text,onClick}:{title:string;text:string;onClick:()=>void}){return <button className="action-card" onClick={onClick}><b>{title}</b><small>{text}</small><i>›</i></button>}
function Agenda({pro}:{pro:boolean}){const[rows,setRows]=useState<any[]>([]),[services,setServices]=useState<any[]>([]),[loading,setLoading]=useState(true);useEffect(()=>{Promise.all([getAppointments(),getServices()]).then(([a,s])=>{setRows(a);setServices(s)}).finally(()=>setLoading(false))},[]);if(loading)return <Loading/>;return <><Head eyebrow="AGENDA" title={pro?'Minha agenda':'Agendar atendimento'} subtitle="Dados carregados diretamente do banco."/>{!pro&&<div className="card-grid">{services.map(s=><div className="card" key={s.id}><b>{s.name}</b><p>{s.duration_minutes} min · {money.format(Number(s.price_base||0))}</p></div>)}</div>}<div className="list">{rows.map(r=><div className="list-row" key={r.id}><div><b>{r.clients?.name||'Cliente'}</b><small>{dt.format(new Date(r.starts_at))}</small></div><span className="status">{r.status}</span></div>)}</div>{!rows.length&&<Empty title="Agenda vazia" text="Nenhum agendamento real foi encontrado."/>}</>}
function Clients(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{getClients().then(setRows)},[]);return <><Head eyebrow="CLIENTES" title="Meus clientes" subtitle="Cadastros reais do salão."/>{rows.length?<div className="client-grid">{rows.map(c=><div className="client-card" key={c.id}><b>{c.name}</b><small>{c.email||'Sem e-mail'}</small><small>{c.phone||'Sem telefone'}</small></div>)}</div>:<Empty title="Nenhum cliente" text="Não existem clientes cadastrados."/>}</>}
function Services(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{getServices().then(setRows)},[]);return <><Head eyebrow="SERVIÇOS" title="Serviços" subtitle="Catálogo real."/>{rows.length?<div className="card-grid">{rows.map(s=><div className="card" key={s.id}><b>{s.name}</b><p>{money.format(Number(s.price_base||0))} · {s.duration_minutes} min</p></div>)}</div>:<Empty title="Nenhum serviço" text="Cadastre o primeiro serviço."/>}</>}
function Commands(){const[rows,setRows]=useState<any[]>([]),[clients,setClients]=useState<any[]>([]),[services,setServices]=useState<any[]>([]),[selected,setSelected]=useState<any>(null),[pay,setPay]=useState(false),[err,setErr]=useState('');const reload=()=>Promise.all([getCommands(),getClients(),getServices()]).then(([c,cl,s])=>{setRows(c);setClients(cl);setServices(s)});useEffect(()=>{void reload()},[]);const create=async()=>{try{if(!clients[0]||!services[0])throw new Error('Cadastre cliente e serviço antes de abrir uma comanda.');const c=await createCommand({client_id:clients[0].id});await addCommandItem({command_id:c.id,service_id:services[0].id,description:services[0].name,quantity:1,unit_price:Number(services[0].price_base||0)});await recalculateCommand(c.id);const{error}=await supabase.rpc('transition_command_state',{p_command_id:c.id,p_next:'open'});if(error)throw error;await reload()}catch(e){setErr(e instanceof Error?e.message:'Falha ao criar comanda')}};return <><Head eyebrow="CENTRAL DE COMANDAS" title="Comandas" subtitle="NEW → OPEN → PENDING_PAYMENT → PAID → CANCELLED" action={<button className="primary-btn" onClick={create}>+ Nova comanda</button>}/>{err&&<ErrorBox text={err}/>}<div className="list">{rows.map(c=><div className="list-row" key={c.id}><div><b>{c.clients?.name||'Cliente'}</b><small>{money.format(Number(c.total||0))}</small></div><div><span className="status">{c.status}</span>{c.status==='open'&&<button className="outline-btn" onClick={()=>{setSelected(c);setPay(true)}}>Fechar / Enviar pagamento</button>}</div></div>)}</div>{!rows.length&&<Empty title="Nenhuma comanda" text="Crie uma comanda real para iniciar o fluxo."/>}{pay&&selected&&<Payment command={selected} close={()=>setPay(false)} done={()=>{setPay(false);void reload()}}/>}</>}
function Payment({command,close,done}:{command:any;close:()=>void;done:()=>void}){const[method,setMethod]=useState<'PIX'|'CREDIT_CARD'|'PDV'>('PIX'),[inst,setInst]=useState(1),[ref,setRef]=useState(''),[busy,setBusy]=useState(false),[err,setErr]=useState('');const gross=Number(command.total||0),ranking=method==='PDV'?[]:calculateBestPaymentGateway(gross,method,inst);const submit=async()=>{setBusy(true);try{if(method==='PDV'){if(!ref.trim())throw new Error('Informe a referência da maquininha.');const{data,error}=await supabase.from('payment_methods').select('id').eq('name','PDV / Maquininha').maybeSingle();if(error)throw error;if(!data)throw new Error('Método PDV não cadastrado.');await markCommandPaidPdv(command.id,data.id,ref.trim(),gross)}else{const best=ranking[0];await closeCommandOnline(command.id,best.gatewayId,best.feeTotal,best.netAmount,inst)}done()}catch(e){setErr(e instanceof Error?e.message:'Falha no pagamento.')}finally{setBusy(false)}};return <div className="modal-back"><div className="modal"><div className="modal-head"><div className="eyebrow">PAGAMENTO · {money.format(gross)}</div><button className="icon-btn" onClick={close}>×</button></div><div className="pay-method-grid">{(['PIX','CREDIT_CARD','PDV'] as const).map(x=><button key={x} className={method===x?'pay-method selected':'pay-method'} onClick={()=>setMethod(x)}>{x==='CREDIT_CARD'?'Cartão':x==='PDV'?'PDV / Maquininha':'PIX'}</button>)}</div>{method==='CREDIT_CARD'&&<label>Parcelas<select value={inst} onChange={e=>setInst(Number(e.target.value))}>{Array.from({length:12},(_,i)=><option key={i+1}>{i+1}</option>)}</select></label>}{method==='PDV'?<label>Referência da maquininha<input value={ref} onChange={e=>setRef(e.target.value)} placeholder="NSU / código"/></label>:<div className="ranking">{ranking.map(r=><div className={`rank-row ${r.isBestOption?'best':''}`} key={r.gatewayId}><b>{r.gatewayName}</b><span>{money.format(r.netAmount)} líquido<br/><small>taxa {money.format(r.feeTotal)}</small></span></div>)}</div>}{err&&<ErrorBox text={err}/>}<div className="modal-actions"><button className="outline-btn" onClick={close}>Cancelar</button><button className="primary-btn" disabled={busy} onClick={submit}>{busy?'Processando…':method==='PDV'?'Confirmar pagamento presencial':'Enviar para pagamento'}</button></div></div></div>}
function Finance(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{getFinancial(200).then(setRows)},[]);const revenue=rows.filter(r=>r.type==='revenue'&&r.status==='paid').reduce((s,r)=>s+Number(r.amount||0),0);return <><Head eyebrow="FINANCEIRO" title="Financeiro" subtitle="Lançamentos reais."/><div className="metric-grid"><Metric v={money.format(revenue)} l="Receitas pagas"/><Metric v={rows.length} l="Lançamentos"/></div><div className="list">{rows.map(r=><div className="list-row" key={r.id}><div><b>{r.description}</b><small>{r.type} · {r.status}</small></div><strong>{money.format(Number(r.amount||0))}</strong></div>)}</div>{!rows.length&&<Empty title="Sem lançamentos" text="O financeiro está vazio."/>}</>}
function Gateways(){return <><Head eyebrow="GATEWAYS" title="Gateways" subtitle="Ranking financeiro no fechamento."/><div className="card-grid">{['PagBank','Asaas','Stripe','PicPay','Nubank'].map(x=><div className="card" key={x}><b>{x}</b><p className="muted">Adapter disponível para configuração segura no backend.</p></div>)}</div></>}
function Simple({title,text}:{title:string;text:string}){return <><Head eyebrow="ED & DU" title={title}/><Empty title={title} text={text}/></>}
