const map:Record<string,string>={'Agendar atendimento':'Agenda','Meus agendamentos':'Agenda','Meus documentos':'Docs','Suporte':'Suporte','Nova Comanda':'Comandas','Minha Agenda':'Agenda','Meus Clientes':'Clientes','Relatórios':'Relatórios'}
document.addEventListener('click',event=>{const button=(event.target as HTMLElement)?.closest?.('.action-card') as HTMLElement|null;if(!button)return;const label=button.querySelector('b')?.textContent?.trim()||'';const target=map[label];if(!target)return;const nav=[...document.querySelectorAll<HTMLButtonElement>('.side-nav,.bottom-nav button')];nav.find(x=>x.textContent?.trim()===target)?.click()})
